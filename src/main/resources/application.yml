logging:
  level.: DEBUG

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/xxxxx
    username: root
    password: sql2016.
    driver-class-name: com.mysql.cj.jdbc.Driver
  mail:
    default-encoding: UTF-8
    host: smpt.gmail.com
    username: w.felipe.gutierrez@gmail.com
    password: Lost2011.
    port: 587
    properties.mail.smtp:
      auth: true
      starttls.enable: true
      ssl.trust: smtp.gmail.com

server:
  port: 8865

security:
  jwt:
    token:
      uri: /authenticate
      expiration: 604800
      secretKey: mySecret
